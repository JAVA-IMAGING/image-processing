package org.ccode.asset.ctn.image;

import org.ccode.asset.ctn.logging.Logger;
import org.ccode.asset.ctn.logging.LoggerBuilder;

public class AlignColorImages {
    private static final Logger _logger = LoggerBuilder.defaultLogger(AlignColorImages.class.getName());

    public static void alignTheImages(String nameOfPrimaryDocument, String[] nameofAlignmentImages) {
        _logger.info(nameOfPrimaryDocument);
    }
}
